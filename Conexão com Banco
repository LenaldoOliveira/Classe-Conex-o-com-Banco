import java.sql.Connection;
import java.sql.DriverManager;

public class Conexao {
    public static Connection conectar() {
        try {
            return DriverManager.getConnection("jdbc:sqlite:sistema.db");
        } catch(Exception e) {
            e.printStackTrace();
            return null;
        }
    }
}
